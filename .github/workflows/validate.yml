name: validate

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  health:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Proje sağlık kontrolü
        run: ./scripts/validate.sh