name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run test suite
        run: bash scripts/test.sh

      - name: Check maturity score
        run: bash scripts/score-maturity.sh --strict 90